<!DOCTYPE html>
<html>
  <head>
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= 'Dev: ' if Rails.env.development? %><%= content_for?(:title) ? yield(:title) : "Chili Cookoff Voter" %></title>
    <meta name="description" content="<%= content_for?(:description) ? yield(:description) : "Chili Cookoff Voter" %>">

    <%= stylesheet_link_tag "application", media: "all" %>
    <%= csrf_meta_tags %>
  </head>
  <body>
    <%# application layout styled for Twitter Bootstrap 3.0 %>
    <header>
      <%= render 'layouts/navigation' %>
    </header>
    <main role="main">
      <div class="container">
         <div class="row">
           <div class="col-md-12">
             <%= render 'layouts/messages' %>
             <%= yield %>
           </div>
         </div>
      </div>
    </main>

    <%= javascript_include_tag "application" %>
  </body>
</html>
